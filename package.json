{
  "name": "starbooks",
  "version": "1.0.0",
  "description": "Full-stack book catalog app with React frontend and Express backend",
  "author": "Giovanna Alves",
  "license": "ISC",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix front",
    "start:server": "npm run dev --prefix server",
    "start:front": "npm start --prefix front",
    "dev": "concurrently \"npm run start:server\" \"npm run start:front\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
